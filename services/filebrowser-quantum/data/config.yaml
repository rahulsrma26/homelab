server:
  cacheDir: /srv/extra/tmp/filebrowser-cache # using the data volume so it can persist across restarts
  sources:
    - path: /srv/media
      config:
        defaultEnabled: true
    - path: /srv/extra
      config:
        defaultEnabled: true
