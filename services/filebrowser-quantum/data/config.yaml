server:
  cacheDir: /home/filebrowser/data/tmp # using the data volume so it can persist across restarts
  sources:
    - path: /srv/media
      config:
        defaultEnabled: true
    - path: /srv/extra
      config:
        defaultEnabled: true
