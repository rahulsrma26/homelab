Web-based remote desktop and terminal access via Apache Guacamole.

Services:
  termix  :8080  Web UI for SSH, RDP, and VNC sessions
  guacd   :4822  Guacamole daemon — handles protocol translation (internal only)

Before deployment:
  .env
