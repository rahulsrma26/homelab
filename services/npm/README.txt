Nginx Proxy Manager — reverse proxy and TLS termination for all services.

Services:
  npm  :80/:443  HTTP/HTTPS proxy
       :81       Web UI for managing proxy hosts and SSL certificates

Before deployment:
  .env  (optional — defaults to ./data)

After first boot:
  Default login: admin@example.com / changeme — change immediately.
